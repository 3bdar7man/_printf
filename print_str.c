#include "main.h"
#include <string.h>
/**
 * printfp - jjdjdjdjkdk
 * kdokeokekoe keokdokoko
 *
 * Return: void
 */

void printfp(void)
{
	if (str == 0)
		str = "(null)";
	length = strlen(str);

	return (str);
}
